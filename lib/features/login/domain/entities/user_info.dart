abstract class NewUserInfo {
  String? firstName;
  String? lastName;
  DateTime? dob;
  String? password;

  NewUserInfo(
      {required firstName, required lastName, required dob, required password});
}
